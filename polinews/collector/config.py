"""Backward-compatible re-exports from the unified polinews.config."""
# All collector modules import from here — no changes needed.
from polinews.config import (  # noqa: F401
    BACKOFF_SECONDS,
    CANONICAL_RESOLVE_BACKOFF_SECONDS,
    CANONICAL_RESOLVE_MAX_RETRIES,
    CANONICAL_RESOLVE_TIMEOUT_SECONDS,
    DB_PATH as DEFAULT_DB_PATH,
    DB_PATH,
    DEDUP_WINDOW_HOURS,
    DEFAULT_FEEDS_CSV,
    DEFAULT_LOG_DIR,
    DEFAULT_STATE_PATH,
    KEYPOINTS_BACKOFF_SECONDS,
    KEYPOINTS_MAX_RETRIES,
    KEYPOINTS_MAX_STORIES_PER_RUN,
    KEYPOINTS_MODEL,
    KEYPOINTS_TIMEOUT_SECONDS,
    MAX_RETRIES,
    OLLAMA_BASE_URL,
    QUIZ_POOL_BACKOFF_SECONDS,
    QUIZ_POOL_ENABLED,
    QUIZ_POOL_GENERATOR_VERSION,
    QUIZ_POOL_MAX_RETRIES,
    QUIZ_POOL_MAX_STORIES_PER_RUN,
    QUIZ_POOL_MIN_SIZE,
    QUIZ_POOL_MODEL,
    QUIZ_POOL_TARGET_SIZE,
    QUIZ_POOL_TIMEOUT_SECONDS,
    REQUEST_TIMEOUT_SECONDS,
    STALE_THRESHOLD,
    USER_AGENT,
)
