# Specs Notes

## Feed Registry Freeze (W1-02)

Effective date: 2026-03-14

Policy lock for `feed_registry_v1.csv`:
- Naming convention is locked to `feed_<topic>_<country>`.
- New feed additions or feed modifications are allowed only during scheduled checkpoints.

Operational note:
- This freeze is process governance. It does not force URL changes when current feeds are valid.
